# Configuración del servidor
PORT=8080

# Base de datos principal (tenant por defecto)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=tu_password
DB_NAME=gestio_db

# Base de datos master (autenticación y permisos)
MASTER_DB_NAME=gestio_master

# SSL (activar en producción con Neon/Render)
DB_SSL=false

# JWT Configuration
# IMPORTANTE: Cambiar esta clave en producción (mínimo 32 caracteres)
JWT_SECRET=clave-secreta-cambiar-en-produccion-min-32-chars
JWT_EXPIRES_IN=24h

# Usuario Master (acceso total al sistema)
MASTER_USERNAME=DaVinci
MASTER_PASSWORD=Vitruvio
